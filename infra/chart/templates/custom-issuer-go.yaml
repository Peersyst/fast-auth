{{- if .Values.customIssuerGo.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: custom-issuer-go-{{ .Values.environment }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: api
    port: {{ .Values.customIssuerGo.service.port }}
    targetPort: {{ .Values.customIssuerGo.service.targetPort }}
  selector:
    app: custom-issuer-go-{{ .Values.environment }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.customIssuerGo.deployment.name }}-{{ .Values.environment }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.customIssuerGo.deployment.name }}-{{ .Values.environment }}
spec:
  replicas: {{ .Values.customIssuerGo.deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.customIssuerGo.deployment.name }}-{{ .Values.environment }}
  template:
    metadata:
      annotations:
        timestamp: {{ .Values.timestamp }}
      labels:
        app: {{ .Values.customIssuerGo.deployment.name }}-{{ .Values.environment }}
    spec:
      serviceAccountName: peersyst-secrets-iamserviceaccount
      volumes:
      - name: custom-issuer-go-secrets
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: custom-issuer-go-{{ .Values.environment }}-secrets
      containers:
      - name: custom-issuer-go
        image: ghcr.io/peersyst/fast-auth-custom-issuer-go:{{ .Values.tagCustomIssuerGo }}
        imagePullPolicy: Always
        resources:
          requests:
            memory: {{ .Values.customIssuerGo.deployment.memoryRequest }}
            cpu: {{ .Values.customIssuerGo.deployment.cpuRequest }}
          limits:
            memory: {{ .Values.customIssuerGo.deployment.memoryLimit }}
            cpu: {{ .Values.customIssuerGo.deployment.cpuLimit }}
        ports:
        - containerPort: {{ .Values.customIssuerGo.deployment.port }}
        volumeMounts:
        - name: custom-issuer-go-secrets
          mountPath: "/mnt/secrets"
          readOnly: true
        env:
        - name: CONFIG_ENV
          value: {{ .Values.environment }}
        {{- range $k, $v := .Values.customIssuerGo.secretProviderClass.secrets }}
        - name: {{ $v }}
          valueFrom:
            secretKeyRef:
              name: {{ $.Values.customIssuerGo.secretProviderClass.objectAlias }}
              key: {{ $v }}
        {{ end }}
      imagePullSecrets:
      - name: regcred
---
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: custom-issuer-go-{{ .Values.environment }}-secrets
  namespace: {{ $.Values.namespace }}
spec:
  provider: aws
  parameters:
    region: us-east-1
    objects: |
      - objectName: {{ $.Values.customIssuerGo.secretProviderClass.objectName }}
        objectType: secretsmanager
        jmesPath:
        {{- range $k, $v := .Values.customIssuerGo.secretProviderClass.secrets }}
          - path: {{ $v }}
            objectAlias: {{ $k }}
        {{ end }}
  secretObjects:
  - secretName: {{ $.Values.customIssuerGo.secretProviderClass.objectAlias }}
    type: Opaque
    data:
    {{- range $k, $v := .Values.customIssuerGo.secretProviderClass.secrets }}
    - objectName: {{ $k }}
      key: {{ $v }}
    {{ end }}
{{- end }}
