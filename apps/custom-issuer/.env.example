# Custom Issuer Service Environment Variables
# Copy this file to .env and update with your actual values

# =============================================================================
# REQUIRED - Application will not start without these
# =============================================================================

# Path to the RSA private key file used for signing issued tokens
# Example: /path/to/keys/signing-key.pem
KEY_PATH=

# Path to the RSA public key file used for validating input JWT tokens
# Example: /path/to/keys/validation-public-key.pem
VALIDATION_PUBLIC_KEY_PATH=

# URL of the issuer service (will be included as 'iss' claim in issued tokens)
# Example: http://localhost:3000
ISSUER_URL=

# =============================================================================
# OPTIONAL - Application will use defaults if not set
# =============================================================================

# Port number for the HTTP server (default: 3000)
PORT=3000

# Comma-separated list of allowed CORS origins
# If not set, CORS will be disabled (no cross-origin requests allowed)
# Example: http://localhost:3000,https://example.com,https://app.example.com
ALLOWED_ORIGINS=

# Base directory path for key files (default: current working directory)
# All key file paths must be within this directory to prevent path traversal attacks
# Example: /secure/keys/directory
KEYS_BASE_PATH=
